.sidebar
  .header
    .header__user-name
      = current_user.name
      .header__icon
        = link_to do
          = fa_icon 'edit'
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog'

  .groups
    .groups__name
      groupname
    .groups__message
      message
